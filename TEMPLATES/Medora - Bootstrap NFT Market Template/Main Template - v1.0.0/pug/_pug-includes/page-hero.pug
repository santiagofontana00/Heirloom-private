if (heroPage)
    // HERO SECTION
    section.py-5.bg-dark.position-relative
        #particles-js
        .container.py-5.mt-5.z-index-20
            h1.text-center #{pageTitle}
            
            // Breadcrumb
            nav(aria-label='breadcrumb')
                ol.breadcrumb.justify-content-center
                    li.breadcrumb-item
                        a.text-decoration-none.d-flex.align-items-center(href='#') 
                            i.las.la-home.la-sm.me-1
                            | Home
                    li.breadcrumb-item.active(aria-current='page') #{pageTitle}